{
  "name": "readai-to-notion-worker",
  "version": "1.0.0",
  "description": "Read.ai to Notion worker for Cloudflare Workers",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy --config wrangler-worker.toml"
  },
  "devDependencies": {
    "wrangler": "^4.42.2"
  }
}
